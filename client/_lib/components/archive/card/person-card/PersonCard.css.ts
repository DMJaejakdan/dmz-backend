import { theme } from '#/design/theme.css';
import { style } from '@vanilla-extract/css';

export const person_card_base = style({
  padding: '0.5rem',
  width: 320,
  height: 200,
  display: 'flex',
  // 임시
  border: '1px solid white',
});
