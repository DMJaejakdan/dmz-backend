import { style } from '@vanilla-extract/css';

export const card_container = style({
  position: 'relative',
  display: 'flex',
  flexFlow: 'column',
  alignItems: 'center',
  width: 'fit-content',
  height: 'fit-content',
  cursor: 'pointer',
});
export const img = style({});
